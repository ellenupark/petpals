<nav class="navbar is-light" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a class="navbar-item" href="/">
      <img src="/logo.png"  height="28">
    </a>

    <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div class="navbar-menu">
    <div class="navbar-start">
      <a class="navbar-item" href="/">
        Home
      </a>

      <a class="navbar-item" href="/pets">
        Browse
      </a>

      <% if user_signed_in? %>
        <a class="navbar-item" href="/users/<%= current_user.id %>">
            Profile
        </a>
      <% end %>

    </div>

    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
            <% if !user_signed_in? %>
                <a class="button is-primary" href="/users/sign_up">
                    <strong>Sign up</strong>
                </a>
                <a class="button is-light" href="/users/sign_in">
                    Log in
                </a>
            <% end %>

            <% if user_signed_in? %>
                <a class="button is-primary" href="/users/sign_out" data-method="delete">
                    <strong>Sign Out</strong>
                </a>
            <% end %>
      </div>
    </div>
  </div>
</nav>